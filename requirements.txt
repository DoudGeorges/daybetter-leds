# BLE communication
bleak>=0.21,<4

# Screen capture (Windows GPU-accelerated, optional on other platforms)
dxcam>=0.3; sys_platform == "win32"

# Screen capture (cross-platform fallback)
mss>=9

# Numeric / color science
numpy>=1.24
